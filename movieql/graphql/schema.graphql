type Movie {
  id: Int!
  title: String!
  year: Int!
  rating: Float!
  runtime: Int!
  summary: String!
  medium_cover_image: String
}

type Query {
  getAllMovies: [Movie]
  getDetailMovie(movieId: Int!): Movie
  getSuggestionMovies(movieId: Int! ): [Movie]
}
